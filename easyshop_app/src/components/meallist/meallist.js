import React from "react";

export class MealList extends React.Component {
    render() {
        // Need to create a way to add meals to a Meal List (Shopping List)
        // and display them all here
        return (
            <div>Meal List works!</div>
        )
    }
}